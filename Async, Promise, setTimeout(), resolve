
2621. Sleep
Easy

Given a positive integer millis, write an asynchronous function that sleeps for millis milliseconds. It can resolve any value.

Note that minor deviation from millis in the actual sleep duration is acceptable.

 

Example 1:

Input: millis = 100
Output: 100
Explanation: It should return a promise that resolves after 100ms.
let t = Date.now();
sleep(100).then(() => {
  console.log(Date.now() - t); // 100
});
Example 2:

Input: millis = 200
Output: 200
Explanation: It should return a promise that resolves after 200ms.
 

Constraints:

1 <= millis <= 1000

=======================================================================================================
/**
 * @param {number} millis
 * @return {Promise}
 */
My code:
async function sleep(millis) {
    return new Promise(resolve => setTimeout(resolve, millis));

}
// setTimeout(callback, delay);- callback: the function to run after the delay || delay: how long to wait (in milliseconds)

//example:
//setTimeout(() => console.log("Hello"), 1000); --> waits 1 sec and then prints Hello
//new Promise() Creates a promise that resolves after the delay
//======================================================================================================
/** 
 * let t = Date.now()
 * sleep(100).then(() => console.log(Date.now() - t)) // 100
 */
